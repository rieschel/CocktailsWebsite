import {createTheme} from '@mui/material/styles';

const theme = createTheme( {
    palette: {
        background: {
            main: "#DEFFD2",
        },
        primary: {
            main: "#03045e",
        },
        secondary: {
            main: "#e2eafc",
        },
    },
});

export default theme;