import {createTheme} from '@mui/material/styles';

const theme = createTheme( {
    palette: {
        background: {
            default: "#d7e3fc",
        },
        primary: {
            main: "#03045e",
        },
        secondary: {
            main: "#e2eafc",
        },
    },
});

export default theme;